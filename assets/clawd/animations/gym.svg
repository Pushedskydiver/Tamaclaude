<svg xmlns="http://www.w3.org/2000/svg" viewBox="-3 -9 21 25" width="168" height="200">
  <!--
    Clawd: gym (Bash)

    Generated against assets/clawd/base.svg under the contract in
    docs/ANIMATION.md. Every base element appears below at its original
    coordinates and colours; all motion is CSS applied by ID.

    First animation built to the rule that came out of `thinking`: on this
    geometry PROPS CARRY THE MOTION and limbs stay exactly where the base puts
    them (docs/ANIMATION.md §What pose swapping cannot do). No claw is
    repositioned to hold the bar — the bar travels, the body dips under it, and
    the lift reads from the prop rather than the limb.

    The bar must make CONTACT at the bottom of the rep. An earlier version
    racked it three units clear of the claws and it read as a barbell floating
    above a crab, because a prop that never touches the character is not being
    held by it. At chest height the plates overlap the claw line and the
    reading changes completely.

    No scale transform anywhere. A squash would be the obvious way to show
    load, but docs/ANIMATION.md's pixel arithmetic covers translation only, and
    a scaled edge lands between pixels. The body dips a whole unit instead.

    Loop: 1.0s at 8fps. The bar travels 8 units over 4 frames — exactly 2
    units, 16 device pixels, per frame — then back. Periods of 1.0s only. Nothing runs
    at 0.125s.
  -->
  <defs>
    <style>
      /* One press per loop: racked at frame 0, full extension at frame 4. */
      #fx-barbell {
        animation: press 1s infinite linear;
      }

      /* Held for two frames at the bottom of the rep rather than interpolated
         — a fractional dip would soften the whole silhouette. It covers frames
         0 and 1, the start of the drive; the bar is also at its lowest on
         frame 7, on the way down, where he is absorbing rather than pushing. */
      #master-group {
        animation: take-the-load 1s infinite steps(1);
      }

      /* Eyes squeeze shut on the two frames the bar crosses them. Contact at
         chest height means the bar must pass the face on its way overhead —
         frames 1 and 7, where it lands on row 9 and the eyes occupy rows 8
         and 9. Left alone it sliced both eyes in half for a frame each way,
         the same intermittent-occlusion fault the laptop lid had.

         An earlier version also translated the eyes up to track the bar. That
         looked good and moved them into the bar's path on frame 2, creating a
         third collision the squint did not cover — a tracking motion that
         walks a part into a moving prop is worse than no tracking. */
      #left-eye,
      #right-eye {
        animation: watch-the-bar 1s infinite steps(1);
      }

      /* Exertion marks, binary opacity. Visible frames 2-5, which is the
         drive plus the first frame of the descent — effort outlasting the
         movement by a frame reads better than effort stopping at lockout. */
      #fx-effort {
        animation: effort 1s infinite steps(1);
      }

      /* Eight units over four frames is two units — sixteen whole device
         pixels — per frame. Racked at chest height where the plates overlap
         the claws, pressed overhead where they clear the head. */
      @keyframes press {
        0%   { transform: translateY(0); }
        50%  { transform: translateY(-8px); }
        100% { transform: translateY(0); }
      }

      @keyframes take-the-load {
        0%   { transform: translateY(1px); }
        25%  { transform: translateY(0); }
        100% { transform: translateY(0); }
      }

      @keyframes watch-the-bar {
        0%    { opacity: 1; }
        12.5% { opacity: 0; }
        25%   { opacity: 1; }
        87.5% { opacity: 0; }
        100%  { opacity: 1; }
      }

      @keyframes effort {
        0%   { opacity: 0; }
        25%  { opacity: 1; }
        75%  { opacity: 0; }
        100% { opacity: 0; }
      }
    </style>
  </defs>

  <!-- ── base geometry: assets/clawd/base.svg, verbatim ────────────────── -->
  <rect id="ground-shadow" x="3" y="15" width="9" height="1" fill="#000000" opacity="0.5"/>

  <g id="master-group">
    <g id="body-color-group" fill="#DE886D">
      <rect id="torso" x="2" y="6" width="11" height="7"/>
      <rect id="left-arm" x="0" y="9" width="2" height="2"/>
      <rect id="right-arm" x="13" y="9" width="2" height="2"/>

      <rect id="outer-left-leg" x="3" y="13" width="1" height="2"/>
      <rect id="inner-left-leg" x="5" y="13" width="1" height="2"/>
      <rect id="inner-right-leg" x="9" y="13" width="1" height="2"/>
      <rect id="outer-right-leg" x="11" y="13" width="1" height="2"/>
    </g>

    <g id="eyes-color-group" fill="#000000">
      <rect id="left-eye" x="4" y="8" width="1" height="2"/>
      <rect id="right-eye" x="10" y="8" width="1" height="2"/>
    </g>
  </g>

  <!-- ── additive props ────────────────────────────────────────────────── -->
  <!-- Barbell at its lowest position; the whole group translates. Plates sit
       clear of the torso top edge at y=6 when the bar is down. -->
  <g id="fx-barbell" fill="#8B949E">
    <rect x="3" y="11" width="9" height="1"/>
    <rect x="2" y="10" width="1" height="3"/>
    <rect x="12" y="10" width="1" height="3"/>
  </g>

  <g id="fx-effort" fill="#F0883E">
    <rect x="0" y="7" width="1" height="1"/>
    <rect x="14" y="7" width="1" height="1"/>
  </g>
</svg>
